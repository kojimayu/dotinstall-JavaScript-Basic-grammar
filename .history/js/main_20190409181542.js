'use strict';

let price = 500;

// price = price + 100;

price += 100;

price *= 2; //price = price * 2

price = price + 1;
price += price;
price++;

price--; //price -= 1

console.log(price);
